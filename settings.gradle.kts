pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "wheelpicker"

include(":wheelpicker")
include(":wheelpicker-compose")
include(":samples")
