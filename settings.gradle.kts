pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}
rootProject.name = "snappicker"

include(":snappicker-compose")
include(":samples:android-compose")
